services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    volumes:
      - ./backend/db:/app/db:Z
    restart: unless-stopped
  frontend:
    build: ./frontend
    ports:
      - "3001:80"
    restart: unless-stopped
